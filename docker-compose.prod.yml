# services:
#   resume-app:
#     # (optional) pull a prebuilt image instead of building on the VPS:
#     # image: ghcr.io/youruser/resume-app:2025-10-05
#     env_file:
#       - .env
#     ports: []            # removes the dev port publish
#     expose:
#       - "3000"           # visible *inside* docker only
#     networks:
#       - boltluna-net
#     restart: "unless-stopped"

# networks:
#   boltluna-net:
#     external: true


services:
  resume-app:
    build:
      context: .
      dockerfile: dockerfile   # or Dockerfile if that’s your filename
    container_name: resume-app
    env_file:
      - .env
    ports:
      - "3001:3000"   # host:container
    restart: unless-stopped

