# services:
#   resume-app:
#     # (optional) pull a prebuilt image instead of building on the VPS:
#     # image: ghcr.io/youruser/resume-app:2025-10-05
#     env_file:
#       - .env
#     ports: []            # removes the dev port publish
#     expose:
#       - "3000"           # visible *inside* docker only
#     networks:
#       - boltluna-net
#     restart: "unless-stopped"

# networks:
#   boltluna-net:
#     external: true



services:
  resume-app:
    build:
      context: .
      dockerfile: dockerfile   # <- you have a lowercase "dockerfile"
    container_name: resume-app
    env_file:
      - .env
    expose:
      - "3000"                 # internal-only
    networks:
      - boltluna-net
    restart: unless-stopped

networks:
  boltluna-net:
    external: true
